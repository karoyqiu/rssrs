// @ts-nocheck

// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/


export const commands = {
/**
 * 添加监视关键字。
 */
async dbAddWatchKeyword(keyword: string) : Promise<boolean> {
    return await TAURI_INVOKE("db_add_watch_keyword", { keyword });
},
/**
 * 删除种子。
 */
async dbDeleteSeed(seedId: number) : Promise<boolean> {
    return await TAURI_INVOKE("db_delete_seed", { seedId });
},
/**
 * 删除监视关键字。
 */
async dbDeleteWatchKeyword(keyword: string) : Promise<boolean> {
    return await TAURI_INVOKE("db_delete_watch_keyword", { keyword });
},
/**
 * 获取所有种子。
 */
async dbGetAllSeeds() : Promise<Seed[]> {
    return await TAURI_INVOKE("db_get_all_seeds");
},
/**
 * 获取文章。
 */
async dbGetArticles(filters: ArticleFilters) : Promise<ArticleResult> {
    return await TAURI_INVOKE("db_get_articles", { filters });
},
/**
 * 获取设置。
 */
async dbGetSetting(key: string) : Promise<string> {
    return await TAURI_INVOKE("db_get_setting", { key });
},
/**
 * 获取未读数量。
 */
async dbGetUnreadCount(seedId: number | null) : Promise<number> {
    return await TAURI_INVOKE("db_get_unread_count", { seedId });
},
/**
 * 获取监视关键字列表。
 */
async dbGetWatchList() : Promise<string[]> {
    return await TAURI_INVOKE("db_get_watch_list");
},
/**
 * 插入种子。
 */
async dbInsertSeed(name: string, url: string) : Promise<boolean> {
    return await TAURI_INVOKE("db_insert_seed", { name, url });
},
/**
 * 将文章标记为已读或未读。
 */
async dbReadArticle(itemId: number, read: boolean) : Promise<boolean> {
    return await TAURI_INVOKE("db_read_article", { itemId, read });
},
/**
 * 全部标记为已读
 */
async dbReadAll(seedId: number | null) : Promise<boolean> {
    return await TAURI_INVOKE("db_read_all", { seedId });
},
/**
 * 修改设置。
 */
async dbSetSetting(key: string, value: string) : Promise<boolean> {
    return await TAURI_INVOKE("db_set_setting", { key, value });
},
/**
 * 更新种子。
 */
async dbUpdateSeed(seedId: number, name: string, url: string) : Promise<boolean> {
    return await TAURI_INVOKE("db_update_seed", { seedId, name, url });
},
/**
 * 更新种子排序。
 */
async dbUpdateSeedRank(seedId: number, rank: number) : Promise<boolean> {
    return await TAURI_INVOKE("db_update_seed_rank", { seedId, rank });
},
/**
 * 下载指定 URL 的数据
 */
async download(url: string, referer: string | null) : Promise<string> {
    return await TAURI_INVOKE("download", { url, referer });
}
}

/** user-defined events **/


export const events = __makeEvents__<{
articleReadEvent: ArticleReadEvent,
seedAddEvent: SeedAddEvent,
seedNewEvent: SeedNewEvent,
seedUnreadCountEvent: SeedUnreadCountEvent,
watchlistChangeEvent: WatchlistChangeEvent
}>({
articleReadEvent: "article-read-event",
seedAddEvent: "seed-add-event",
seedNewEvent: "seed-new-event",
seedUnreadCountEvent: "seed-unread-count-event",
watchlistChangeEvent: "watchlist-change-event"
})

/** user-defined constants **/



/** user-defined types **/

/**
 * 文章
 */
export type Article = { 
/**
 * ID
 */
id: number; 
/**
 * 种子 ID
 */
seed_id: number; 
/**
 * 种子名称
 */
seed_name: string; 
/**
 * 标题
 */
title?: string | null; 
/**
 * 作者
 */
author?: string | null; 
/**
 * 描述
 */
desc?: string | null; 
/**
 * 链接
 */
link: string; 
/**
 * 发布时间，UNIX Epoch
 */
pub_date: number; 
/**
 * 是否未读
 */
unread: boolean }
export type ArticleFilters = { seedId: number | null; cursor: string | null; limit: number | null; search: string | null; unreadOnly: boolean | null }
/**
 * 文章未读事件
 */
export type ArticleReadEvent = { 
/**
 * 文章 ID
 */
id: number; 
/**
 * 是否未读
 */
unread: boolean }
export type ArticleResult = { articles: Article[]; nextCursor: string | null }
/**
 * 种子
 */
export type Seed = { 
/**
 * ID
 */
id: number; 
/**
 * 名称
 */
name: string; 
/**
 * URL
 */
url: string; 
/**
 * 
 * * 图标
 * *
 * * TODO: 保存 URL 或 base64，估计是后者
 * 
 */
favicon?: string | null; 
/**
 * 更新周期，分钟
 */
interval: number; 
/**
 * 最近抓取时间，UNIX Epoch
 */
last_fetched_at: number; 
/**
 * 最近抓取是否成功
 */
last_fetch_ok: boolean; 
/**
 * 文章保留时长
 */
reserved_in_days?: number | null; 
/**
 * 排序
 */
rank: number | null }
/**
 * 种子添加事件
 */
export type SeedAddEvent = null
/**
 * 新种子事件
 */
export type SeedNewEvent = { 
/**
 * 种子 ID
 */
id: number | null; 
/**
 * 未读数量
 */
unreadCount: number }
/**
 * 种子未读数量事件
 */
export type SeedUnreadCountEvent = { 
/**
 * 种子 ID
 */
id: number | null; 
/**
 * 未读数量
 */
unreadCount: number }
/**
 * 关注列表修改事件
 */
export type WatchlistChangeEvent = null

/** tauri-specta globals **/

import {
	invoke as TAURI_INVOKE,
	Channel as TAURI_CHANNEL,
} from "@tauri-apps/api/core";
import * as TAURI_API_EVENT from "@tauri-apps/api/event";
import { type WebviewWindow as __WebviewWindow__ } from "@tauri-apps/api/webviewWindow";

type __EventObj__<T> = {
	listen: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.listen<T>>;
	once: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.once<T>>;
	emit: null extends T
		? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
		: (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>;
};

export type Result<T, E> =
	| { status: "ok"; data: T }
	| { status: "error"; error: E };

function __makeEvents__<T extends Record<string, any>>(
	mappings: Record<keyof T, string>,
) {
	return new Proxy(
		{} as unknown as {
			[K in keyof T]: __EventObj__<T[K]> & {
				(handle: __WebviewWindow__): __EventObj__<T[K]>;
			};
		},
		{
			get: (_, event) => {
				const name = mappings[event as keyof T];

				return new Proxy((() => {}) as any, {
					apply: (_, __, [window]: [__WebviewWindow__]) => ({
						listen: (arg: any) => window.listen(name, arg),
						once: (arg: any) => window.once(name, arg),
						emit: (arg: any) => window.emit(name, arg),
					}),
					get: (_, command: keyof __EventObj__<any>) => {
						switch (command) {
							case "listen":
								return (arg: any) => TAURI_API_EVENT.listen(name, arg);
							case "once":
								return (arg: any) => TAURI_API_EVENT.once(name, arg);
							case "emit":
								return (arg: any) => TAURI_API_EVENT.emit(name, arg);
						}
					},
				});
			},
		},
	);
}
